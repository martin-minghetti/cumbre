# === Required ===
DATABASE_URL=postgres://user:pass@host/db?sslmode=require
SESSION_SECRET=generate_with_openssl_rand_hex_32
CART_SECRET=generate_with_openssl_rand_hex_32
ORDER_TOKEN_SECRET=generate_with_openssl_rand_hex_32
OWNER_EMAIL=owner@example.com

# === Setup-only (delete after first login) ===
INITIAL_OWNER_EMAIL=owner@example.com
INITIAL_OWNER_PASSWORD=changeme123

# === Optional: Mercado Pago ===
MP_ACCESS_TOKEN=
MP_WEBHOOK_SECRET=
PAYMENT_MODE=simulated

# === Optional: Email ===
RESEND_API_KEY=

# === Optional: Asset generation ===
REPLICATE_API_TOKEN=
